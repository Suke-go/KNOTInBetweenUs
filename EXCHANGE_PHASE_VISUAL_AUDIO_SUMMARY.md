# Exchange Phase 演出まとめ

## 概要

Exchange Phase（交換フェーズ）は、2人の参加者が互いの心音を交換し、体験する60秒間のフェーズです。視覚と音響の両面で「交換」の概念を表現しています。

## フェーズの基本情報

- **継続時間**: 60秒
- **前のフェーズ**: FirstPhase（60秒後、自動遷移）
- **次のフェーズ**: Mixed（60秒後、自動遷移）
- **遷移**: ベル音と共に自動遷移

---

## 1. 視覚演出

### 1.1 背景

- **色**: 深い青色（RGB: 5, 6, 12）
  - FirstPhaseと同様の背景色を使用
  - `bgEnhancement = 0.5f`による微調整
- **テクスチャ**: 微かなノイズテクスチャ（`drawSubtleNoise`）
- **注**: 星空や銀河の表現は使用しない（シンプルな背景）

### 1.2 光の移動演出

#### 基本構成
- **2分割構成**: 左側と右側に2つの光が独立して表示
- **左側の光**: 参加者2の心拍に同期（パートナーの心音を視覚化）
- **右側の光**: 参加者1の心拍に同期（パートナーの心音を視覚化）

#### 移動パターン
- **交換サイクル**: 60秒間で完全な交換サイクル
- **前半（0.0-0.5）**: 
  - 左側の光: 左側（32%位置）→ 中央（50%位置）
  - 右側の光: 右側（68%位置）→ 中央（50%位置）
- **後半（0.5-1.0）**: 
  - 左側の光: 中央 → 右側（68%位置）へ移動（交換）
  - 右側の光: 中央 → 左側（32%位置）へ移動（交換）

#### イージング
- **イーズイン・アウト曲線**: `0.5f - 0.5f * cos(movementProgress * π)`
- 滑らかな移動を実現

#### 光の強度
- **Glow強度**: 0.9-1.0の範囲で常に明るく保つ（可視性を確保）
- **Bloom強度**: 2.0-2.5の範囲
- **心拍同期**: 各光は対応する参加者の心拍に同期して点滅
  - `flash = 0.85f + 0.15f * (smoothstep(0.0f, 0.3f, phase) - smoothstep(0.3f, 1.0f, phase))`

### 1.3 波紋（Ripple）演出

#### 生成タイミング
- **開始**: Exchange phase開始30秒以降から波紋を生成
- **生成条件**: 
  - 各参加者の心拍位相が0.3未満の時
  - 最小ビート間隔（0.4秒）を超えた時

#### 波紋の特性
- **生成位置**: 各光の現在位置から直接生成
  - 右側の光（参加者1の心音）から波紋
  - 左側の光（参加者2の心音）から波紋
  - **注**: 現在の実装では、中央からの波紋（3つ目）は生成されていません
    - 光が中央で交差するタイミング（exchangeProgress ≈ 0.5）に中央からも波紋を生成する機能は未実装
- **拡散速度**: 200 pixels/秒
- **最大寿命**: 2.5秒
- **フェードアウト**: 最大寿命の60%以降からフェードアウト開始
- **色**: 温かみのある白色（RGB: 251, 245, 231）
- **線幅**: 2.5px

#### 波紋の強度
- **時間による増加**: `smoothstep(0.0f, 10.0f, timeInExchange)`
- Exchange phase開始から10秒かけて徐々に強度が増加

---

## 2. 音響演出

### 2.1 空間音響システム

#### 交換時間
- **交換期間**: 20秒間かけて音源が移動・交換
- **イージング**: イーズイン・アウト曲線により滑らかな移動

#### 音源位置の構造体
```cpp
struct SoundSourcePosition {
    float distance;    // 距離 (m)
    float azimuth;     // 方位角 (度): -180 to +180, 0 = 正面
    float elevation;   // 仰角 (度): -90 to +90, 0 = 水平
};
```

### 2.2 参加者1の視点

#### 自分の心音
- **開始位置**: 中央（1.0m, 0°）
- **終了位置**: 右側遠方（3.0m, +90°）
- **移動**: 20秒間かけて右側へ移動し、距離も遠くなる

#### パートナーの心音（参加者2）
- **開始位置**: 遠方（3.0m, -90°）
- **終了位置**: 中央（1.0m, 0°）
- **移動**: 20秒間かけて左側から中央へ移動し、距離も近くなる

### 2.3 参加者2の視点

#### 自分の心音
- **開始位置**: 中央（1.0m, 0°）
- **終了位置**: 左側遠方（3.0m, -90°）
- **移動**: 20秒間かけて左側へ移動し、距離も遠くなる

#### パートナーの心音（参加者1）
- **開始位置**: 遠方（3.0m, +90°）
- **終了位置**: 中央（1.0m, 0°）
- **移動**: 20秒間かけて右側から中央へ移動し、距離も近くなる

### 2.4 距離に応じた音の変化

#### 距離減衰（Distance Attenuation）
- **計算方法**: 逆二乗則
  ```cpp
  attenuation = (refDistance²) / (distance²)
  ```
- **基準距離**: 1.0m（最小距離）
- **最大距離**: 3.0m
- **減衰範囲**: 0.1-1.0に制限
- **効果**: 距離が2倍になると音量が1/4になる

#### スペクトラル変化（Distance Filtering）
- **ローパスフィルター**: 距離が遠くなるほど高周波成分が減衰
- **カットオフ周波数**: 
  - 近距離（1.0m）: 20000Hz
  - 遠距離（3.0m）: 2000Hz
- **実装**: 簡易的な1次IIRフィルター
- **効果**: 遠方の音は低音が強調され、近くの音は明瞭に聞こえる

### 2.5 方位角に基づくパンニング

#### 空間パンニング
- **計算方法**: 
  ```cpp
  panLR = sin(azimuth * π / 180.0)
  ```
- **範囲**: -1.0（左）から +1.0（右）
- **効果**: 方位角に応じて自然なパンニングを実現

#### 処理順序
1. 距離減衰
2. スペクトラル変化（ローパスフィルター）
3. パンニング

---

## 3. 視覚と音響の同期

### 3.1 時間軸の関係

- **視覚の交換サイクル**: 60秒間で完全な交換サイクル（ループ）
- **音響の交換**: 20秒間で完全な交換（1回のみ）
- **波紋の開始**: 30秒以降から生成

### 3.2 同期ポイント

- **光の位置**: 各参加者の心拍に同期して点滅
- **波紋の生成**: 各参加者の心拍位相が0.3未満の時に生成
- **音源の移動**: 視覚の光の移動と連動して音源位置が更新

---

## 4. 実装ファイル

### 視覚演出
- `src/ofApp.cpp`: 
  - `drawExchangeScene()`: Exchange phaseの描画
  - `drawExchangeRipplesFromLights()`: 波紋の描画
  - `drawHeartbeatLight()`: 心拍同期の光の描画

### 音響演出
- `src/audio/AudioRouter.h`: 空間音響の構造体と関数宣言
- `src/audio/AudioRouter.cpp`: 
  - `updateSpatialAudio()`: 音源位置の更新
  - `calculateDistanceAttenuation()`: 距離減衰の計算
  - `applyDistanceFiltering()`: スペクトラル変化の適用
  - `calculateSpatialPan()`: 空間パンニングの計算
  - `applySpatialAudio()`: 空間音響処理の適用

### 統合
- `src/ofApp.cpp`: `update()`関数から`updateSpatialAudio()`を呼び出し

---

## 5. 技術的な詳細

### 5.1 イージング関数

```cpp
float easedProgress = 0.5f - 0.5f * std::cos(exchangeProgress * glm::pi<float>());
```

- **入力**: 0.0-1.0の進行度
- **出力**: 0.0-1.0のイージング済み進行度
- **特性**: イーズイン・アウト（滑らかな開始と終了）

### 5.2 波紋の生成ロジック

```cpp
if (phase < 0.3f && (nowSeconds - lastRippleTime) >= minBeatInterval) {
    // 波紋を生成
    ripples_.push_back({
        nowSeconds,
        lightPosition,
        0.0f
    });
}
```

- **位相チェック**: 0.3未満で新しいビートとして検出
- **間隔チェック**: 最小ビート間隔（0.4秒）を超えた時のみ生成

### 5.3 距離減衰の計算

```cpp
float attenuation = (refDistance * refDistance) / (distance * distance);
attenuation = std::max(0.1f, std::min(1.0f, attenuation));
```

- **逆二乗則**: 物理的に正確な距離減衰
- **制限**: 0.1-1.0の範囲に制限（完全に消音されないように）

### 5.4 スペクトラル変化の計算

```cpp
float cutoffFreq = maxCutoff - (maxCutoff - minCutoff) * distanceNormalized;
float rc = 1.0f / (2.0f * π * cutoffFreq);
float alpha = dt / (rc + dt);
state = state + alpha * (sample - state);
```

- **1次IIRフィルター**: 簡易的なローパスフィルター
- **動的カットオフ**: 距離に応じてカットオフ周波数が変化

---

## 6. ユーザー体験の流れ

1. **FirstPhaseから遷移**: ベル音と共に自動的にExchange phaseへ
2. **開始（0-20秒）**: 
   - 視覚: 左右の光が中央へ移動
   - 音響: 自分の心音が中央から遠方へ、パートナーの心音が遠方から中央へ
3. **交換中（20-30秒）**: 
   - 視覚: 光が中央で交差し、反対側へ移動開始
   - 音響: 交換が完了（20秒で完了）
4. **波紋開始（30秒以降）**: 
   - 視覚: 心拍に同期した波紋が生成され始める
   - 波紋の強度が徐々に増加
5. **継続（30-60秒）**: 
   - 視覚: 光の交換サイクルが継続（60秒で1サイクル）
   - 音響: 交換後の状態を維持
   - 波紋: 心拍に同期して継続的に生成
6. **Mixed phaseへ遷移**: 60秒後、自動的にMixed phaseへ

---

## 7. 今後の拡張予定

### HRTFによるバイノーラルレンダリング
- **現状**: 簡易的な空間音響（距離減衰とパンニング）を使用
- **将来**: HRTFライブラリ（libmysofa）の統合
  - SOFAファイルの読み込み
  - HRIRテーブルの構築
  - リアルタイムでの畳み込み処理
  - よりリアルな3D空間音響体験

### 中央からの波紋（3つ目）の実装
- **現状**: 左側と右側の光からのみ波紋を生成
- **将来の拡張**: 光が中央で交差するタイミング（exchangeProgress ≈ 0.5）に、中央からも波紋を生成
  - 交換の瞬間を視覚的に強調
  - 2つの光が交差する際の「共鳴」を表現
  - 実装方法: `exchangeProgress`が0.4-0.6の範囲で、かつ心拍イベントが発生した時に中央から波紋を生成

### パフォーマンス最適化
- フィルター処理の最適化
- メモリ使用量の最適化

---

## 8. 関連ドキュメント

- `EXCHANGE_AUDIO_IMPLEMENTATION.md`: 音響演出の詳細な実装サマリー
- `USER_EXPERIENCE_PHASES.md`: ユーザー体験の全体像
- `IMPLEMENTATION_SUMMARY.md`: 実装の概要
- `TECHNICAL_IMPLEMENTATION_REQUIREMENTS.md`: 技術的な要件

