# Exchangeフェーズ音響演出実装サマリー

## 実装完了日
2024年（現在）

## 実装内容

### 1. 空間音響システムの実装

#### 1.1 音源位置の構造体
- **SoundSourcePosition**: 3D空間上の音源位置を表現
  - `distance`: 距離 (m)
  - `azimuth`: 方位角 (度): -180 to +180, 0 = 正面
  - `elevation`: 仰角 (度): -90 to +90, 0 = 水平

#### 1.2 音源位置の更新（updateSpatialAudio）
- **Exchangeフェーズ**: 20秒間かけて音源が移動・交換
  - **参加者1の視点**:
    - 自分の心音: 中央(1.0m, 0°) → 右側遠方(3.0m, +90°)
    - パートナーの心音: 遠方(3.0m, -90°) → 中央(1.0m, 0°)
  - **参加者2の視点**:
    - 自分の心音: 中央(1.0m, 0°) → 左側遠方(3.0m, -90°)
    - パートナーの心音: 遠方(3.0m, +90°) → 中央(1.0m, 0°)
  - イーズイン・アウト曲線により滑らかな移動

### 2. 距離に応じた音の変化

#### 2.1 距離減衰（calculateDistanceAttenuation）
- **逆二乗則**: 距離が2倍になると音量が1/4になる
- 最小距離（1.0m）を基準に、最大距離（3.0m）での減衰を計算
- 減衰の範囲を0.1-1.0に制限

#### 2.2 スペクトラル変化（applyDistanceFiltering）
- **ローパスフィルター**: 距離が遠くなるほど高周波成分が減衰
- カットオフ周波数: 20000Hz（近距離） → 2000Hz（遠距離）
- 簡易的な1次IIRフィルターを使用

### 3. 方位角に基づくパンニング

#### 3.1 空間パンニング（calculateSpatialPan）
- 方位角を-1.0（左）から+1.0（右）のパン値に変換
- 正弦関数を使用して自然なパンニングを実現

#### 3.2 空間音響処理（applySpatialAudio）
- 距離減衰 → スペクトラル変化 → パンニングの順で処理
- 各参加者と各音源の組み合わせごとに独立して処理

### 4. AudioRouterへの統合

#### 4.1 Exchangeフェーズのルーティング
- 自分の心音とパートナーの心音を両方ミックス
- 各音源に対して空間音響処理を適用
- 参加者1と参加者2で異なる音源位置を適用

#### 4.2 ofAppへの統合
- `updateSpatialAudio`を`update`関数から呼び出し
- フレームごとに音源位置を更新

## 実装ファイル

- `src/audio/AudioRouter.h`: 空間音響の構造体と関数宣言
- `src/audio/AudioRouter.cpp`: 空間音響の実装
- `src/ofApp.cpp`: AudioRouterの呼び出し

## 技術的な詳細

### 距離減衰の計算
```cpp
float attenuation = (refDistance * refDistance) / (distance * distance);
```

### スペクトラル変化の計算
```cpp
float cutoffFreq = maxCutoff - (maxCutoff - minCutoff) * distanceNormalized;
float alpha = dt / (rc + dt);
state = state + alpha * (sample - state);
```

### 空間パンニングの計算
```cpp
float panLR = std::sin(azimuth * glm::pi<float>() / 180.0f);
```

## 次のステップ

### 残りの実装項目

1. **HRTFによるバイノーラルレンダリング**（将来の拡張）:
   - HRTFライブラリの統合（libmysofa）
   - SOFAファイルの読み込み
   - HRIRテーブルの構築
   - リアルタイムでの畳み込み処理

2. **パフォーマンス最適化**:
   - フィルター処理の最適化
   - メモリ使用量の最適化

3. **ユーザーテスト**:
   - 空間音響の動作確認
   - ユーザーのフィードバック収集

## 注意事項

- 現在の実装は簡易的な空間音響（距離減衰とパンニング）を使用
- HRTFによるバイノーラルレンダリングは将来の拡張として予定
- 距離減衰とスペクトラル変化により、音源が遠方へ移動する感覚を実現

## テスト項目

- [x] 音源位置の計算が正しく動作する
- [x] 距離減衰が正しく適用される
- [x] スペクトラル変化が正しく適用される
- [x] 空間パンニングが正しく動作する
- [ ] ユーザーテスト: 音源の移動が感じられる
- [ ] ユーザーテスト: 距離感が正しく感じられる

